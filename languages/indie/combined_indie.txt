# combined_indie.txt — Indie translations for Pokémon Unbound (EN→Indie)
#
# Same format as combined_fr.txt:  <offset_hex>: <text_indie>
#   \n  -> line break        \l -> <0xFA> (scroll)      \p -> <0xFB> (clear)
# Lines starting with '#' and blank lines are ignored.
# Offsets absent from this file keep their English text.
# Last entry wins for a duplicated offset (case-insensitive).
#
# STATUS: seed / work in progress. These entries prove the EN→Indie pipeline
# end to end; the bulk of the script still needs translating. Indie reuses the
# French font, so the full accented glyph set is available.
#
# --- seed entries (short, proven-translatable offsets shared with the DE/IT seeds) ---
# Hindi rendered in Hunterian (Latin) transliteration: the Indie target reuses the
# French font, which has no Devanagari glyphs, so native Devanagari would encode to
# rows of '?' (0xAC fallback). Romanised Latin keeps the Hindi wording encodable.
0x1773FD: Main tumhein bata raha hoon.
0x177489: Abhi tak nahin!
0x183509: Tum mujhe peet dete ho.
