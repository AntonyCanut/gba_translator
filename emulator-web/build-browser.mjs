// No browser bundle needed — emulation runs server-side via mGBA.
// This script is kept as a no-op so existing scripts that call it don't break.
console.log('  build-browser.mjs: no browser bundle needed (mGBA backend)');
