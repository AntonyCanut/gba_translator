# Legacy Scripts (Deprecated)

This folder contains legacy extraction/translation scripts kept for reference only.
They are not part of the current pointer-based pipeline.

Use the Makefile targets or the scripts under `src/` for the supported workflow.
