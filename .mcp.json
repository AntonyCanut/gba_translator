{
  "$schema": "https://json.schemastore.org/mcp.json",
  "//": "Serveurs MCP partagés par les assistants compatibles (Claude, Ollama). Lancement via npx/uvx, sans clé en dur.",
  "mcpServers": {
    "filesystem": {
      "//": "Accès fichiers borné au dépôt — lecture des artefacts output/ et du code src/.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
      "tools": ["read_file", "list_directory", "search_files"]
    },
    "image-tools": {
      "//": "Conversion d'images (PNG/JPG/WebP/AVIF) — utile pour les snapshots Playwright et les sprites GBA.",
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-image-tools"],
      "tools": ["image_convert"]
    },
    "git": {
      "//": "Inspection de l'historique git en lecture seule (log, diff, blame).",
      "command": "uvx",
      "args": ["mcp-server-git", "--repository", "."],
      "tools": ["git_log", "git_diff", "git_show"]
    }
  }
}
