---
name: unbound-daycare-withdraw-menu-help-first-word-freeze
description: "Day-Care 'reprendre ton Pokémon' freeze = menu cursor-help string 0x416244 hangs le word-wrap ; PAS de règle largeur/1er-mot (réfutée in-engine) — allowlist de strings replayées ; fix TOUTES langues (IT=Torna, DE=texte EN), tests unit+e2e par langue (B-187)"
metadata:
  node_type: memory
  type: project
  originSessionId: 58fad1a2-e873-4cb8-a7a5-b0754123507c
---

**⚠️ CORRECTION MAJEURE (5ᵉ réouverture, 2026-07-06) — la théorie « 1er mot large » ci-dessous est FAUSSE.** L'user rouvrait « marche en FR mais pas dans les autres langues, freeze/restart continue ». Cause réelle : un `make build-it/de` frais applique bien la source, mais les strings « fix » de la passe précédente **figent quand même** — vérifié in-engine par isolation (patch 0x416244 d'une ROM FR-base atteignable + `decide.mts`, seuls les verdicts `reached=true` fiables, `pkill mgba`+attendre port 55234 vide entre runs). **AUCUNE règle largeur/longueur/1er-mot/newline ne tient** : sur la MÊME save, `"Torna al menu precedente."`=OK mais le PLUS COURT `"Vai al menu precedente."`=FREEZE ; `"Va au menu\nprécédent."`=OK mais `"Zum vorigen\nMenü."`=FREEZE ; `"Zurück."` (7 car.)=FREEZE ; le texte EN source (29 car.)=OK. Le seul oracle = **replay in-engine**, traiter chaque string comme coupable jusqu'à preuve. **Fix retenu (toutes langues, vérifié sur les ROMs réelles buildées)** : FR garde `"Va au menu\nprécédent."` (OK) ; **IT revert → `"Torna al menu precedente."`** (l'original shippé, OK — la passe précédente l'avait cassé en `Vai`) ; **DE → texte EN source `"Go back to the\nprevious menu."`** (aucune formulation allemande testée ne survit ; c'est la suggestion communauté « remets le texte anglais ») ; indie hérite EN (OK). Tests : `test_daycare_withdraw_menu_help.py` réécrit en **allowlist par langue** (fr/it/de/indie, SAFE_STRINGS + KNOWN_FREEZE, la garde `word_width≤20` était fausse) + NOUVEAU e2e `tests/e2e/test_daycare_withdraw_replay.py` (replay in-engine par langue via `scripts/verify_daycare_no_freeze.mts` language-agnostic + fixture `tests/fixtures/saves/daycare_withdraw.sav`, self-check : détecte FREEZE sur DE re-cassé). NB pipeline OK (source→ROM marche ; les ROMs it/de gitignored prébuild étaient juste PÉRIMÉES) ; `ü` collapse en `u` dans ce chemin (cosmétique). Voir [[diagnosing-unbound-freezes-skill]] (section « other freeze class »). ⬇️ Le reste ci-dessous est l'HISTORIQUE (théorie 1er-mot périmée) :

**B-187 « Freeze reprendre Pokémon » RÉSOLU.** Choisir « Oui » à « Veux-tu reprendre ton Pokémon ? » (Pension/Day-Care, mémé) fige le jeu dans **toutes** les builds langue (pas la source EN — prouvé: `englishrom.gba` + même `.sav` retire le Pokémon sans figer). Régression de build, PAS un pointeur ho-oh.

**Cause racine (bisection in-engine byte-par-byte)** : la chaîne d'aide-curseur du menu de sélection à **0x416244** — EN « Go back to the\nprevious menu. » / FR « Revenir au\nmenu précédent. ». Cette boîte d'aide **ne tolère qu'un PREMIER MOT très court**. Largeurs px (`src.core.dialogue_linewrap.word_width`) : premier mot ≤ ~12 px (Go/Va/Au) = OK ; ≥ 27 px (Aller=27, Retour=34, Revenir=38) = **FREEZE**. Contre-intuitif : la largeur des mots SUIVANTS est sans effet (« précédent » 51 px, « previous » 44 px sur la 2ᵉ ligne = OK). Seul le mot de tête compte. Ni é (0x1b), ni longueur totale, ni position du terminateur ne comptent — testés, tous FREEZE tant que le 1er mot est large.

**Fix (garde la trad FR)** : reword `combined_fr.txt` 0x416244 → « **Va au menu\nprécédent.** » (1er mot « Va » 12 px). Patché AUSSI le ROM buildé octet-par-octet (0x416244) car un rebuild concurrent régénère depuis l'ancienne source (pattern [[singularity-build-resets-worktree]]) — au rebase, `git checkout --ours` sur la .gba puis réappliquer le patch. Test de non-régression build-indépendant : `tests/test_daycare_withdraw_menu_help_fr.py` (assert `word_width(premier_mot) ≤ 20`). Commit gba_translator@unbound. Suite rapide 1420 passed.

**LEÇON HARNESS CAPITALE** : le mashing A « aveugle » (`basefreeze.mts` : 5×A puis A-Oui) donne des **FAUX NÉGATIFS** quand un revert change la longueur d'un texte de menu → le timing d'entrée décale, l'appui « Oui » rate, le jeu reste sagement sur la boîte Oui/Non → verdict `frozen=false` MENTEUR. Ça a corrompu une 1ʳᵉ bisection entière (fausse piste 0x416800-0x417000). Harness FIABLE : lire `gStringVar4` (0x02021D18) et n'avancer qu'une fois le prompt confirmé par ses octets (FR « Veux-tu » `d0 d9 e9 ec` OU EN « Will you » `d1 dd e0 e0`), PUIS Oui, PUIS détecter le hang (timeout bridge KEYFRAMES = vrai freeze). `verdict:'OK',reached:true` est fiable ; un timeout seul peut être flaky (tuer mgba entre runs). Voir [[diagnosing-unbound-freezes-skill]], [[unbound-mgba-harness-flags-invalid]].

**3ᵉ RÉOUVERTURE B-187 (« Ta fix sur toutes les langues ? ») — fix GÉNÉRALISÉ IT+DE (2026-07-06).** Le ticket disait « freeze dans TOUTES les langues » ; seul FR avait été corrigé. Mesuré `word_width` du 1er mot à 0x416244 dans chaque `languages/<code>/combined_*.txt` : **IT « Torna » 28px, ES « Regresa » 40px, DE « Zurück » 34px** — tous ≥27px = FREEZE (FR « Va » 12px OK, EN « Go » 12px OK). Reword IT→« **Vai** al menu\nprecedente. » (16px), DE→« **Zum** vorigen\nMenü. » (18px) ; les deux tiennent in-place (24/18 o ≤ budget EN 30 o). ES = source de référence communautaire (pas un build maintenu), indie = pas d'entrée → hors périmètre. Build IT/DE via `make build-it/de` (worktree : symlink `output/extracted`+`output/differences` du checkout principal), **décodé 0x416244 dans la ROM buildée** = octets corrects. Test de régression `test_daycare_withdraw_menu_help_fr.py` **paramétré FR/IT/DE** (garde `word_width(1er mot) ≤ 20`). Commit gba_translator@unbound. **PIÈGE REBASE** : pendant le build, B-211 a avancé unbound (encodage umlaut DE slots 0xF1-0xF6 + combined_de.txt) → rebase propre (mes lignes 0x416244 intactes) PUIS **rebuild+re-décode DE** obligatoire. NB : ü/ö dans le chemin combined→ROM s'encodent `e9`/`e3` (bas), pas F1-F6 (= autre encodeur « real dialogue ») — vérifié : voisin non touché « Was möchtest… » 0x416262 encode ö=`e3` pareil, donc « Menü »=`...e9` est cohérent et rend correct. Suite complète 1879 passed, 41 skipped.

Il peut exister d'AUTRES chaînes d'aide-menu à premier mot large (même famille que [[unbound-pc-item-from-menu-help-2line]] 0x4185xx) susceptibles de figer d'autres menus — non auditées ici. (NB : l'heuristique « 1er mot ≤ 20 px » sur-alerte massivement hors de ce menu précis — Pokédex 41 px, Joueur 35 px rendent OK partout ; ne PAS auditer toute la région 0x416xxx avec ce garde.)

**2ᵉ RÉOUVERTURE B-187 (« ne crash plus mais il restart. Donc c'est KO ») — c'était ENCORE une build périmée, prouvé octet + in-engine (2026-07-06).** Fix « Va au menu » landé dans combined_fr sur unbound par commit **74a3886 le 2026-07-06 11:18**. TOUTES les ROMs FR du user dans ~/Downloads sont datées **20-27 juin** et décodent encore « Revenir au\nmenu précédent. » à 0x416244 = **cassées, antérieures au fix** (`GenedRom-fr.gba`, `GenedRom.fr (2).gba` du 27/06, etc.). Le user n'a JAMAIS eu de build avec le fix. Reproduction décisive sur SA propre save : `/tmp/repro/repro.sav` (save Pension, Magmar/Lucario ; 5×A → boîte Oui/Non). Harness `/tmp/repro/decide.mts` (gate sur octets prompt « Veux » `d0 d9 e9 ec`, puis A-Oui, distingue **CRASH** = mGBA meurt/`Command timeout: KEYFRAMES` vs **FREEZE** = hash écran identique). Verdicts : `repro.gba` (« Revenir ») → **CRASH** (mGBA meurt à l'ouverture du menu) ; ROM courante « Va au menu » → **OK** (0 crash, 0 freeze, 45 A-presses). Donc le fix marche, le diagnostic « débordement rendu 1er mot large » tient. **Résolution du conflit rebase** (la .gba binaire trackée) : unbound a DÉJÀ « Va au menu » baké (source→build), donc `git checkout --ours output/roms/GenedRom-fr.gba` (= version unbound, plus fraîche : Leveinard/Trainer House) puis `git add`+`--continue` — mon commit se réduit au seul docstring du test (le byte-patch manuel de la 1ʳᵉ passe est abandonné, plus nécessaire). **LEÇON** : quand un user rouvre « toujours cassé » sur un fix déjà mergé, décoder 0x<offset> dans SES ROMs `~/Downloads/*.gba` par mtime AVANT de re-diagnostiquer — le vrai bug est la distribution (build périmée), pas le code. Artefacts repro réutilisables laissés dans `/tmp/repro/` (base.gba=EN, repro.gba=cassé, fixnew.gba=fix, decide.mts).

**RÉOUVERTURE B-187 (« ne crash plus mais il restart ») — corrigé, symptôme re-caractérisé.** Ce n'est PAS un simple freeze GetString : c'est un **CRASH / soft-reset dur**. Reproduit in-engine depuis le `.sav` fourni (Downloads/GenedRom-fr.sav, party=4, Magmar/Lucario en pension) : sur la ROM AVANT-fix (« Revenir ») **mGBA MEURT** (« Connection closed ») exactement à la frame d'ouverture du menu de sélection (A#3) → la fenêtre cursor-help de taille fixe déborde, corrompt la mémoire, la ROM se **relance au titre** = le « restart » du user. **PREUVE D'ISOLATION** : patcher UNIQUEMENT 0x416244 d'une build FR propre-et-fonctionnelle en « Revenir au\nmenu précédent. » **réintroduit le crash** à la même frame ; « Va au menu\nprécédent. » → le retrait se termine proprement (2 mons rendus, party pleine gérée « Ton équipe est pleine », sortie du bâtiment OK). Donc **render-overflow reset, PAS un clobber de pointeur** (l'hypothèse « statue Articuno / fix pointeur comme Ho-Oh » du user/communauté était FAUSSE — vérifié : pointeurs daycare→0x1bfae8 intacts, aucun clobber). La build ACTUELLE (source combined_fr + translation_ready 2026-07-06 + ROM committée + rebuild frais) contient déjà « Va au menu » et NE crash PAS → **le user testait une build PÉRIMÉE**. Fix durable : rebuild ROM depuis source (0x416244 baké in-place, abandonne le byte-patch manuel fragile) commit 5a8db7a. **LEÇON HARNESS** : un timeout bridge `FRAMES|N` = mGBA MORT (crash), pas un freeze de rendu ; un vrai freeze de rendu laisse mGBA vivant (screenshots identiques). Distinguer via `catch` + retry advanceFrames après le hang. Save batterie du user : mtime corrèle la réouverture, se charge direct à la scène du screenshot (map devient 1.94 après avoir marché ; getState fige à 0.0/8,5 tant qu'on ne bouge pas — normal).
